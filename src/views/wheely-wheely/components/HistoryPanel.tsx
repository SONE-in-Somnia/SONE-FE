"use client";

import React from "react";
import { RetroButton } from "@/components/RetroButton";

const HistoryPanel = () => {
  return (
    <RetroButton>
      VIEW HISTORY
    </RetroButton>
  );
};

export default HistoryPanel;